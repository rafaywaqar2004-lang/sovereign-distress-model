name: Fetch development indicators + external debt composition (World Bank WDI)

on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install pandas
        run: pip install pandas

      - name: Fetch development indicators
        working-directory: data
        env:
          PYTHONUNBUFFERED: "1"
        timeout-minutes: 40
        run: python -u fetch_development_indicators.py

      - name: Commit fetched data if anything changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/development_indicators.csv data/development_indicators_long.csv
          git diff --staged --quiet && echo "No data changes" && exit 0
          git commit -m "Fetch real development indicators + external debt composition (World Bank WDI)"
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
